baseURL = "https://fc.181861.xyz/"
languageCode = "zh-cn"
title = "第一文明"
theme = "blowfish"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[params]
  description = "这是我在 fc.181861.xyz 上的数字花园"
  colorScheme = "ocean"     # 刚才选好的配色
  showImageZoom = true      # 开启图片缩放
  enableSearch = true       # 开启搜索
  searchType = "fuse"    # 如果 Algolia 还没配好，可以先改回 "fuse"
  
  # --- 网站图标配置 ---
  # Blowfish 会自动在 static/ 目录下寻找这些文件
  favicon = "favicon.png"  
  #favicon = "favicon.png"  # 改为 png 试试，或者保持 favicon.ico 但去掉斜杠
  appleTouchIcon = "apple-touch-icon.png" # 让苹果设备也显示这个图标

  [params.author]
    name = "OneZephyr"
    image = "avatar.png" # 记得把你的头像放在 assets/ 目录下
    headline = "探索数字世界的边界 🚀"
    bio = "对世界充满好奇，正在记录我的技术探索之路。"
    
    # 社交媒体链接（Blowfish 会自动识别并显示图标）
    # 格式为：{ 平台名 = "链接" }
    links = [
      { github = "https://github.com/OneZephyr" },
     # { wechat = "https://mp.weixin.qq.com/s/l9e5Wtgh5hBwxxE5Qxf-NA" },
      { email = "mailto:wzf618@gmail.com" },
      { rss = "index.xml" }
    ]

  [params.header]
    showLogo = true
    showMenu = true

  [params.footer]
    showCopyright = true
    showPoweredBy = true # 这会配合你刚才在 footer.html 写的代码

  [params.article]
    showTableOfContents = true # 开启文章目录
    showDate = true
    showReadingTime = true

# 搜索配置 (如果你用 Algolia)
[params.search.algolia]
  appId = "你的ID"
  apiKey = "你的SearchKey"
  indexName = "fc_blog"

# 评论配置 (Giscus)
[params.comments]
  showComments = true
  engine = "giscus"
  [params.comments.giscus]
    repo = "OneZephyr/FC"
    repoId = "R_xxxxxxxx"
    category = "Announcements"
    categoryId = "DIC_xxxxxxxx"
    mapping = "pathname"
    theme = "preferred_color_scheme"

[params.homepage]
  #layout = "background" # 核心：启用背景布局，支持动态SVG全屏展示
  layout = "profile" # 布局选择：建议选 "profile"，它能同时显示头像、社交图标和文章列表
  layoutBackgroundBlur = true # 可选：给背景添加模糊效果，提升前景文字可读性
  homepageImage = "/background.svg" # 关联动态SVG背景图
  
  # 最近文章的相关配置
  showRecent = true #开启最近文章功能
  recentLimit = 3 # 首页显示多少篇最近文章
  
  # 如果你想让“最近文章”有个标题
  recentTitle = "最新发布"
